[project]
name = "e-mail-spam-detector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "supun theekshana", email = "supuntheekshana00@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "joblib>=1.6.0",
    "scikit-learn>=1.9.0",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
e-mail-spam-detector = "e_mail_spam_detector:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
